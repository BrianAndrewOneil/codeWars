const check = (a, x) => (a.find(element => element == x))==undefined?false:true

//Given an array a and a value x. Check whether the provided array contains the value. Array can contain numbers or strings. X can be either. Return true if the array contains the value, false if not.